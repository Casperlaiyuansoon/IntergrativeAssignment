# Set the server port (default is 8080, you can change it if needed)
server.port=8080

# Spring Boot application configuration
spring.application.name=inventory-service

# Set the context path if you want to access your API with a different URL prefix
# Example: http://localhost:8080/inventory/api/inventory
# server.servlet.context-path=/inventory

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.example.inventory=DEBUG



